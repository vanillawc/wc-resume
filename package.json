{
  "name": "@vanillawc/wc-resume",
  "version": "2.0.32",
  "license": "MIT",
  "author": "Evan Plaice <evanplaice@gmail.com> (https://evanplaice.com/)",
  "description": "A resume.json vanilla web component",
  "keywords": [
    "web-components",
    "vanilla",
    "resume"
  ],
  "repository": "https://github.com/vanillawc/wc-resume/",
  "main": "index.js",
  "scripts": {
    "start": "npx live-server --no-browser --port=5500 --open=dev",
    "lint": "esmtk lint",
    "build": "npm run bundle && npm run minify",
    "bundle": "esmtk bundle src/wc-resume.js index.js",
    "minify": "esmtk minify src/wc-resume.js index.min.js",
    "package": "npx rimraf package && npm pack | tail -n 1 | xargs tar -xf",
    "preversion": "npm run lint",
    "postversion": "git push --follow-tags"
  },
  "devDependencies": {
    "@vanillaes/interpolate": "^1.4.0",
    "esmtk": "^0.4.0"
  },
  "standard": {
    "ignore": [
      "index.js"
    ]
  }
}
